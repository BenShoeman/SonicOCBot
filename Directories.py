import os
import sys
abs_path = sys.path[0]

DATA_DIR = os.path.join(abs_path, "data")
CREDENTIALS_DIR = os.path.join(abs_path, "keys")
MODELS_DIR = os.path.join(abs_path, "text")
FONTS_DIR = os.path.join(abs_path, "fonts")
SONICMAKER_DIR = os.path.join(abs_path, "sonicmaker")
TEMPLATES_DIR = os.path.join(abs_path, "templates")